import classes from './style.module.scss';

export const MakeAuth = (props) => {
    return (
        <div className={classes.make_auth}>
            <div>
                Login
            </div>
        </div>
    )
}
